{% set attributes = attributes.addClass('layout layout-donies') %}
{%
  include "@decanter/layout/grid/two-column/donies/donies.twig" with
  {
    'header': content.header,
    'footer': content.footer,
    'first': content.first,
    'second': content.second,
  }
%}
