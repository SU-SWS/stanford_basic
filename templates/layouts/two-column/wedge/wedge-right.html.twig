{% set attributes = attributes.addClass('layout layout-wedge layout-wedge--right') %}
{%
  include "@decanter/layout/grid/two-column/wedge/wedge.twig" with
  {
    'first': content.first,
    'second': content.second,
  }
%}
