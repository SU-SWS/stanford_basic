{% set attributes = attributes.addClass('layout layout-wedge layout-wedge--left') %}
{%
  include "@decanter/layout/two-column/wedge/wedge.twig" with
  {
    'first': content.first,
    'second': content.second,
  }
%}
