{% set attributes = attributes.addClass('layout layout-cuttoner layout-cuttoner--right') %}
{%
  include "@decanter/layout/grid/three-column/cuttoner/cuttoner.twig" with
  {
    'first': content.first,
    'second': content.second,
    'sidebar': content.third,
    'header': content.header,
    'footer': content.footer,
  }
%}
